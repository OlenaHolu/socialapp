/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'pu1ax1och5r44vjg.public.blob.vercel-storage.com',
            's.gravatar.com'
        ]
    }
};

export default nextConfig;

